task generateReadme {
    doFirst {
        def readmeFile = new File("${project.rootDir}/README.md")
        def content = readmeFile.text
        content = content.replaceAll("'com\\.infinum:retromock:.+?'", "'com.infinum:retromock:${versions.retromock}'")
        readmeFile.setText(content)
    }
}