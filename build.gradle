group = 'co.infinum'
version = "$versions.retromock"

buildscript {
    apply from: 'versions.gradle'
    apply from: "maven.gradle"

    repositories {
        google()
        mavenCentral()
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

